import MusealLayout from "@/components/MusealLayout";
import HeroCarousel from "@/components/HeroCarousel";

const Index = () => {
  return (
    <MusealLayout fullscreen>
      <HeroCarousel />
    </MusealLayout>
  );
};

export default Index;
