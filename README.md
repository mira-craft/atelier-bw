# Overview

A focused, lightweight React + TypeScript page for building an accessible, performant web gallery. It uses Vite for fast local development, Tailwind CSS for styling, and shadcn-ui for UI primitives.